import { apiService } from '../../service/apiService';
import type { ApiResponse } from '../../types/common';

const authUrl = 'auth';

export const authPutAPI = {
  // Removed markSetupComplete - moved to profileAPI
};
